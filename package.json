{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "react and node weather app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start",
    "dev": "concurrently -n 'server,client' -c 'red,green' \"nodemon server.js\" \"npm run client\""
  },
  "keywords": [
    "weather",
    "forcast"
  ],
  "author": "hamdyx",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.0",
    "concurrently": "^7.0.0",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "morgan": "^1.10.0",
    "particles-bg": "^2.5.5"
  }
}
